subprojects {
  apply plugin: 'idea'
  //and standard one
  apply plugin: 'java'

  repositories {
    jcenter()
  }

  dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.+'
  }
}
project(':sup2') {
  dependencies {
    compile project(':sup1')
  }
}
